<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:padding="20dp">

    <ImageButton
        android:id="@+id/btnVoltar"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_alignParentTop="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@android:drawable/ic_menu_revert"
        android:tint="#00FF00" />

    <TextView
        android:id="@+id/titulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:text="📡 DIVULGAÇÃO DE GRUPOS"
        android:textColor="#00FF00"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/txtDescricao"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/titulo"
        android:layout_marginTop="15dp"
        android:gravity="center"
        android:text="Participe de comunidades exclusivas ou divulgue seu próprio grupo para ganhar novos membros. Escolha uma categoria abaixo para explorar."
        android:textColor="#888888"
        android:textSize="12sp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/btnMeusGrupos"
        android:layout_below="@id/txtDescricao"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="10dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <Button
                android:id="@+id/btnAmizade"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_marginBottom="10dp"
                android:backgroundTint="#1A1A1A"
                android:text="🤝 AMIZADE"
                android:textColor="#00FF00"
                android:textStyle="bold" />

            <Button
                android:id="@+id/btnRomance"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_marginBottom="10dp"
                android:backgroundTint="#1A1A1A"
                android:text="❤️ ROMANCE"
                android:textColor="#00FF00"
                android:textStyle="bold" />

            <Button
                android:id="@+id/btnZueira"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_marginBottom="10dp"
                android:backgroundTint="#1A1A1A"
                android:text="🤡 ZUEIRA / HUMOR"
                android:textColor="#00FF00"
                android:textStyle="bold" />

            <Button
                android:id="@+id/btnOutros"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_marginBottom="10dp"
                android:backgroundTint="#1A1A1A"
                android:text="🌐 OUTROS / GERAL"
                android:textColor="#00FF00"
                android:textStyle="bold" />

        </LinearLayout>
    </ScrollView>

    <Button
        android:id="@+id/btnMeusGrupos"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_above="@id/btnAnunciar"
        android:layout_marginBottom="10dp"
        android:backgroundTint="#1A1A1A"
        android:text="🛠️ MEUS GRUPOS (EDITAR/REMOVER)"
        android:textColor="#FFFFFF"
        android:textStyle="bold" />

    <Button
        android:id="@+id/btnAnunciar"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="10dp"
        android:backgroundTint="#00FF00"
        android:text="📢 DIVULGAR NOVO GRUPO"
        android:textColor="#000000"
        android:textStyle="bold" />

</RelativeLayout>